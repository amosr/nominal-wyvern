// From reviewer 2, requires expansion
name List { z =>
  type Element <= Top
}

name Integer { z =>

}

name IntList { z =>
  type Element = Integer
}

subtype IntList <: List

assert IntList <: List {type Element = Integer }