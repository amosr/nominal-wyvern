type A { z =>
  type X <= Unit
}

type B { z =>
  type X <= Unit
}

subtype A extends B

// subtype A { type X <= Unit } extends BT

// assert A {} <: B {}
// assert A { type X <= Top } <: B {}
// assert A <: B { type X <= Top }
// assert A { type X <= Bot } <: B { type X <= Top }
// assertNot A { type X <= Top } <: B { type X <= Bot }

val ok : A { type X >= Unit } = new A {this =>
  type X = Unit
}
0